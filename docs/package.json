{
  "name": "btmux-khi-docsite",
  "private": true,
  "scripts": {
    "build": "hugo --source . --destination public --cleanDestinationDir",
    "serve": "hugo server --source . --buildDrafts"
  },
  "devDependencies": {
    "autoprefixer": "10.4.27",
    "postcss-cli": "11.0.1"
  }
}
