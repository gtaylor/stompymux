+++
title = "AUDITORIUM"
description = "Require a speak lock in a location"
keywords = ["auditorium"]
article_tags = ["flags"]
+++

# AUDITORIUM (`b`)

Marks a location as an auditorium. Speaking there requires the speaker to pass
the location's `speak` lock. The normal GAGGED restriction still applies.
