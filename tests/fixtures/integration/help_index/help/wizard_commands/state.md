+++
title = "@state"
description = "Inspect persistent object state"
keywords = ["@state", "object state"]
article_tags = ["wizard_commands"]
wizard_only = true

show_index_for_article_tags = ["state_switches"]
index_style = "list_with_description"
+++

# @state

`@state` groups the Wizard-only persistent object state administration commands
under one command. Type `@state` by itself to see a short list of switches, or
use one of the forms indexed below.
