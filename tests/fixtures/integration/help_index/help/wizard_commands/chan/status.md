+++
title = "@chan/status"
description = "Inspect one channel's configuration and activity"
keywords = ["@chan/status"]
article_tags = ["chan_switches"]
weight = 100
wizard_only = true
+++

# @chan/status

Show one channel:

```text
@chan/status <channel>
@chan/status/full <channel>
```

The normal form shows its channel-object description. `/full` shows the player
and object capability flags, attached object, membership, and message count.
