+++
title = "@chan/list"
description = "List available channels"
keywords = ["@chan/list"]
article_tags = ["chan_switches"]
weight = 60
wizard_only = true
+++

# @chan/list

List channels:

```text
@chan/list
@chan/list/full
```

The normal listing shows each visible channel and the description from its
attached channel object. `/full` adds channel flags, the object dbref,
membership, and message counts.
