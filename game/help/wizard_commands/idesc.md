+++
title = "@idesc"
description = "Set the description shown from inside an object"
keywords = ["@idesc", "inside description", "internal description"]
article_tags = ["wizard_commands"]
wizard_only = true
+++

# @idesc

```text
@idesc <object>=<inside description>
```

Sets the description shown to a player inside a non-room object. If it is
unset, the normal description is used. Inside descriptions accept named and
hexadecimal color markup; see `help color`.

